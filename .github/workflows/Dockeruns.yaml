name: Docker workflow

on:
  pull_request:
    branches: CDMA4414-patch-4
    
jobs:
  dockerest:
    runs-on: ubuntu-latest
    container: 
      image: node:19-alpine3.16
    steps:
      - name: nodeversion
        run: node -v
        
    
